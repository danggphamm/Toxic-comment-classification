In this project, we created our own embedding model based on the dataset that contains toxic/nontoxic comments. 
Then we trained different architectures (LTSM/GRU) to learn to classify toxic comments.
The project is written in Python.